#include <stdio.h>

/* (c) Larry Herman, 2021.  You are allowed to use this code yourself, but
   not to provide it to anyone else. */

/* This is a program that sadly has too few comments. :-(  Just add some
   more! */

int main(void) {
  int idx, factorial, n;

  scanf("%d", &n);
  factorial= 1;
  idx= 1;

  while (id <= n)
    factorial *= idx++;

  printf("%d\n", factorial);

  return 0;
}
